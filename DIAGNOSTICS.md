# 診断記録（v0.3.11）

1. アプリの「不具合の診断」で「診断記録をオンにする」を有効にする。
2. 画面共有を開始し、問題が起きる動画を見る。
3. 症状が出たら早めにアプリへ戻り「診断データを書き出す（ZIP）」を押す。
4. Androidの保存画面でダウンロードフォルダなどを選び、保存完了を待つ。
5. ZIPをチャットに添付し、聞こえた症状を短く説明する。

書き出すボタンは記録を停止して、それまで保存した画像とログをまとめます。
押した瞬間だけを撮影する機能ではありません。記録前の画像を遡って取得することもできません。
オンにし直すと前の記録は削除されます。保存画面のキャンセル後も端末内の記録は残ります。
「端末内の診断記録を削除」で消去できます。書き出したZIPは別ファイルなので自分で削除してください。

## 保存内容

- 粗いOCRと再OCRへ実際に渡した画像のPNG（コピー後、別スレッドで保存）
- OCR文字列、行の位置・信頼度、再OCRで選択した文字列
- 時間方向の統合結果、字幕安定化の状態、確定イベント
- 重複判定の採否、TTS用に整形した文章
- 発話リクエスト、待機、投入結果、開始・完了・中断・エラー
- アプリ版、端末・Android版、TTSエンジン、声、速度・音量

ZIPは `manifest.json`, `events.jsonl`, `images/*.png`, `README.txt` から構成します。
`frame_id` はキャプチャ時の単調増加時刻で、YouTubeの再生秒数ではありません。
OCR段階間は `frame_id` と `track_id`、確定と重複判定は `event_id`、
TTS投入と開始・終了は `utterance_id` で照合します。
異なる字幕トラックで `event_id` が重なる場合があるため、本文と時刻も併用してください。

## 保存範囲と制限

初期値はオフです。画像・ログは端末内のバックアップ対象外フォルダへ保存し、自動送信しません。
アプリのプロセス再起動後は記録がオフになりますが、以前の記録は書き出せます。
画像にはOCR範囲内の動画タイトルやUIなども含まれることがあります。

最新の記録を基準に最大120秒、64MiB、2000レコードまで保持します。
上限のいずれかに達すると古い画像と対応レコードをまとめて削除するため、120秒より短くなる場合があります。
負荷を抑えるため、保存待ち画像は同時に2枚、記録タスクは128件までとし、過負荷では省略数を記録します。
`image_status` が `omitted_under_load` の画像は保存されていません。
保持期間の境界では、後続ログから参照する以前の画像がすでに削除されている場合があります。

音声録音、動画ダウンロード、CC字幕取得は行いません。
TTSの開始・終了はOSのコールバックであり、実際のスピーカー音声そのものではありません。
診断記録で画像コピー・保存の負荷が増えるため、問題確認後はオフに戻してください。

保存先選択にはAndroidの [Storage Access Framework](https://developer.android.com/training/data-storage/shared/documents-files) を使用します。

## 検証

DiagnosticStoreTestでUTF-8ログ、画像バイト、時刻・容量上限、再起動後の書き出し、
削除、書き出し失敗を確認します。端末での保存先選択と実動画の採取は別途実機確認が必要です。
