# v0.3.14 字幕発話ライフサイクル

## 問題

v0.3.13では、同じ画面の各行を先に発話した後、結合された複数行字幕を別イベントとして再発話する経路が残っていました。また、字幕はTTSへ渡した時点で既読になり、実際には待ち行列から破棄された場合も再試行できませんでした。

## 変更

- `RowSpeechLedger` を追加し、字幕イベント全体ではなく正規化した行ごとに発話状態を管理
- 状態を待機中、発話中、完了に分離。完了はAndroid TTSの `onDone` でのみ記録
- TTSの中断、エラー、キュー破棄では予約を解放し、OCRがまだ表示を検出できる場合は再確定
- 1行の待機字幕が2〜3行の完全字幕へ拡張された場合、置換される待機行を完全字幕へ移して発話
- 同一字幕内の行順は上から下の入力順を維持。行の並べ替えだけでは再発話しない
- 数字と否定表現の変更は新しい内容として保持
- 完了履歴を60秒、最大256行に制限
- `continuous` 設定を `CONTINUOUS` キューへ正しく渡し、最大2件のFIFOとして動作
- 連続キューが容量超過で破棄した項目にも失敗通知を返し、未発話予約が残らないよう修正
- 診断ログへ予約ID、待機・発話中・完了・解放・置換と台帳件数を追加

## 再現評価

`tools/replay-evaluator/` はv0.3.13の診断ログから前回抽出した正確な重複判定イベントと、数字・否定・短文・再登場の合成対照例を固定しています。旧 `SubtitleEventManager` と新 `RowSpeechLedger` に同じ順序で入力します。

確認項目：

- 既読の第2行を、後から現れた3行結合版で再発話しない
- 結合版で初めて得た第3行は残す
- 第3行だけが後から別トラックで現れても再発話しない
- 数字と否定の変更を抑制しない
- TTS中断で解放された字幕を再試行できる
- 履歴期限後の再登場は発話できる

## 検証の限界

今回の再現評価は保存済みイベント抜粋を使用し、画像からOCRをやり直すものではありません。診断ZIP全体は作業時に取得サーバーがHTTP 502を返したため、字幕選抜やOCR認識率の改善は評価していません。文末の `0`、`go`、`条`など複数文字にまたがるOCR揺れ、異なる位置の字幕グループの識別、字幕消失後の再登場判定は残課題です。Galaxy S25での改善は実機確認が必要です。
