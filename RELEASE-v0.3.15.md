# v0.3.15 — 読み上げ専用音量ブースト

v0.3.14-speech-lifecycle (4bd86e80) を基点とし、字幕判定・既読管理は維持。

- 設定に「読み上げ音量ブースト」オフ／弱（+6 dB）／強（+12 dB）を追加。初期値はオフ、変更は次の発話から適用。
- AudioManagerで生成した専用セッションをKEY_PARAM_SESSION_IDでTTSへ渡し、同じセッションにLoudnessEnhancerを接続。グローバルセッション0は絶対に使わず、端末の音量やブラウザの音量を操作しない。
- 既存0〜100%音量は維持。増幅の実際の効き方は端末・TTSエンジン・出力先による。API成功は実際の音量増加を保証しない。
- 増幅が使えない場合は通常の読み上げを続け、Toastと診断イベントtts_boostで通知。同一設定で発話ごとに再生成せず、オフ→オンで再試行。
- オフ・終了時に効果を解放。強さ切替・無効セッション・非対応・失敗時解放・再試行・二重終了をテスト。

## 実機確認

1. 読み上げ音量を100%にし、ブースト「オフ」で動画と声のバランスを確認。
2. 「弱」を選び、次の発話から声だけが大きくなるか確認。
3. 必要なら「強」。音割れがあれば「弱」へ戻す。
4. オフに戻ること、停止・再開・Bluetoothでも通常どおり読み上げることを確認。
5. 効果がない場合は診断を記録。音量の実機効果は未検証。

## 同じ署名でのビルド

チャット添付の試作APKは別途保存した caption-debug.keystore を使用。
CAPTION_DEBUG_KEYSTORE に鍵の絶対パスを設定して gradle :app:assembleDebug を実行。
旧Actions APKの秘密鍵は不明なため、旧版との上書き互換は保証できない。
署名不一致の場合は設定・診断を書き留めたうえで旧版をアンインストールして導入する。
鍵はリポジトリに含めず、今後の試作APKでも同じバックアップを使用する。
